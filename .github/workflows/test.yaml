name: Run shell commands

on:
  push:
  
jobs:
  my-first-shell-execution:
    runs-on: ubuntu-latest
    steps:
      - name: Display the output of the echo commands
        run: echo "This is the first iteration”